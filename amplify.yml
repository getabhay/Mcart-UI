version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - echo "" > .env.production
            - echo "SEARCH_SERVICE_BASE_URL=$SEARCH_SERVICE_BASE_URL" >> .env.production
            - echo "PRODUCT_SERVICE_BASE_URL=$PRODUCT_SERVICE_BASE_URL" >> .env.production
            - npm run build
      env:
        variables:
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*
          - ~/.npm/**/*
